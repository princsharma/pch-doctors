import { Reviews } from "@/components/sections/reviews";

/** Homepage reviews — same section as the MMJ service page. */
export function HomeReviews() {
  return (
    <div data-service="mmj">
      <Reviews />
    </div>
  );
}
